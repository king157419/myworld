# DECISIONS · 灵境 / Innerscape — 潮汐图书馆 / The Tide Library

> 记录"怎么做"的选择、理由、自纠循环。守住的产品内核见 [CLAUDE.md](./CLAUDE.md) / [AGENTS.md](./AGENTS.md)。
> 本轮是对旧版（雨夜别墅）的**彻底重构**：保留工程管线（持久化 / 数据模型 / 导航框架 / 后处理 / 音频框架），
> 把场景、布局、配色、灯光、天气、音频**全部推倒重做**。参照板见 [references/REFERENCES.md](./references/REFERENCES.md)。

## 一句话概念

**潮汐图书馆 / The Tide Library。** 一座暖灯的读书回廊，立在一片只有几厘米深、却完美映出整片星空的镜面水上。
你"走在星海上"——每一步都在星空的倒影上荡开涟漪，打碎又重聚。映射一个**把记忆当作水来盛放**的内心：
退潮般安静、念旧、辽阔却不孤独。冷的水与天（午夜蓝/银）× 暖的灯与书（琥珀）。

- 思考/文字 = -X 一整面圆弧书墙 + 立在水上的写作台（写下的思考会"沉入水中"）。
- 珍视的物件 = +X 立在水上的发光陈列岛（浮岛基座 + 黄铜柱 + 顶台）。
- 影音 = 后方观星台上的留声机（空间化音乐声源）。
- 上下楼 = 镜面广场（水面层）↔ 后方抬高的观星台（上层），木梯连通。

## 我拒绝做的「默认烂版本」

灰盒子房间 + 三个一样的方块（思考/物件/音乐）+ 平涂均匀光 + 循环嗡鸣 + 声音不空间化 + 相机瞬移 =
换谁都一样的 3D 简历模板。**绝不做这个**（这是上一批的失败形态）。

## 满分定义（一句话）

进门 60 秒内安静下来、并在某刻"倒吸一口气想截图"；十分钟后还不想走——因为它像**某一个具体的人的内心
在缓慢呼吸**，而不是一个换谁都行的模板。

## 完成标准 · 四问的当前答案

1. **哪 3 个瞬间想截图**：① 第一步踏上水面、脚下星海碎开成涟漪；② 暖灯回廊整片倒映在镜水里、悬在星空中间；
   ③ 一本书从水上浮起 / 写下的字随潮浮出水面。（①③ 仍在打磨，见弱项清单。）
2. **别人想不到的一个元素**：地面是**可被你脚步打碎的星空倒影**，"楼层/时间"做成**潮汐**——水既是导航、是时钟、也是情绪。
3. **头 60 秒情绪弧**：黑场只有水声 → 一盏灯亮、照出脚下竟是星空 → 试走一步、涟漪炸开星海（"我站在天上"）→
   远处暖灯回廊浮现 → 第一段字随潮浮出。从"悬浮/迷失"到"被一盏灯接住"。（入场编排待做。）
4. **为何待 10 分钟**：潮汐循环不断改变可达区与浮现内容；写下的字沉入水、下次涨潮再浮起重读；安静、有节律、像会呼吸的房间。

## 技术栈（沿用，版本配对正确）

Vite 8 + React 19 + TS 6（严格档）· three 0.185 · @react-three/fiber 9 · drei 10 · @react-three/postprocessing 3 ·
zustand 5 · Dexie 4（IndexedDB）· Vitest 4。R3F9↔React19↔drei10 配对正确。

## 关键技术决策

| 主题 | 选择 | 理由 |
|---|---|---|
| 星海镜面水（**核心钩子**） | drei `MeshReflectorMaterial`（crisp mirror）+ 自写「脚步涟漪」叠加着色器 | 真实平面反射星空与暖灯；涟漪用环形缓冲 `scene/ripples.ts`，PlayerControls 每跨一步 spawn 一圈 |
| 夜空 | 自写穹顶着色器（渐变 + 银河带）+ 真实星点 Points（会泛光、会被水倒映）+ 低月 | 不下载 HDRI；星点 crisp 才能在水里映出"星海" |
| 潮汐 | `theme.tideOffset/tidePhase`，水面缓慢上下 + 漂浮书随潮起伏 | "水会呼吸"；后续驱动"沉字浮起" |
| 几何真相源 | `theme.ts` 的 LAYOUT（R_COURT 镜面广场 / DECK 观星台 / STEPS 木梯 / PEDESTALS 浮岛 / BOOKWALL 书墙弧） | Gallery 渲染、walk.ts 碰撞、PlayerControls 聚焦共用 |
| 导航 | 沿用第一人称控制器（指针锁定 + WASD + 触屏 + 准心聚焦），改：低掠水面的入场环绕 + 脚步涟漪 | 框架复用，几何换皮 |
| 后处理 | AgX + Bloom（高阈值，只让真高光泛光）+ DOF（梦境景深）+ 暗角 + 极轻颗粒 | 出"镜头感"；Noise 从 0.42 降到 0.12 去掉颗粒噪 |
| 数据契约 | **保留三个 zone id/type**（bookshelf/objects/record），只换几何与皮肤 | zone id 属于数据模型——"世界可由数据完整重建"必须守住 |

## 自纠循环日志（/design-review 之前的内部目检）

**Round 1（基础舞台搭起 → 浏览器实拍 4 次迭代）**
- 发现：水像柏油地、不反射星空（重 blur [320,110] 把反射星点糊没了）—— **最严重，钩子失效**。
  → 修：blur→[0,0]、mixBlur→0、roughness 0.32→0.05、color 提亮一点。水变成 crisp 镜面，书墙/观星台/浮岛清晰倒映。
- 发现：月亮巨大扁平像乒乓球。→ 修：半径 2.4→1.25、加 halo、推到穹顶边、朝向原点。
- 发现：浮岛基座像塑料水桶。→ 重做为"水面小基座 + 细黄铜柱 + 顶台 + 暖光圈"——读作"发光陈列岛"。
- 发现：中央一个暗甜甜圈伪影。→ 定位为反射暗天顶 + blur ringing；blur 归零后消失。
- 发现：月反射是一团发噪的过曝斑。→ Noise 后处理 0.42→0.12；Bloom 阈值 0.16→0.5（只让真高光泛光）。
- 发现：星空太稀、反射没东西可映。→ STAR_COUNT 1700→2600、星点更大、银河带加宽加亮。
- 加：水上**漂浮的旧书**（随潮起伏、缓缓自转）——"把记忆当水盛放"的具象 + 前景不空。
- 加：DOF 收紧 → 梦境景深，星点 bokeh。

**Round 2（接上 zones + 真实音频 → 正式跑了一轮 `/design-review` 子代理评审 → 按分级修）**
- 评审三条 Blocker：① 水不是星镜（核心钩子塌了）；② 画面中央无源白光斑（中央补光在反射里糊成 bug）；③ 斜贯天空的蓝色光束（银河带硬切 + 雾叠出假光柱）。
- **修 ①（最关键）**：放弃"靠 MeshReflectorMaterial 反射稀疏星点"，改用**对称镜像方案**——把整片天空（穹顶+星点+月）再镜像复制一份到水面之下（`Sky` 里 `group scale=[1,-1,1]`），水面换成**半透明菲涅尔玻璃**（`Water`：垂直看几乎透明→看见水下星海，掠射→蓝色水光）。低头任何角度都是一整片密星海。暖灯倒影由水下镜像发光球 `MirrorBeacons` 提供。删掉挡住水下天空的 `SunkenFloor`。星点改为铺满整个上半球（含天顶）、更大更亮、加少量"主星"做锚。
- **修 ②**：删掉悬空的中央补光，换成观星台暖色补光 + 贴水面的低位暖溢光（有源、不糊白斑）。
- **修 ③**：银河带改乳白、加宽、软边、叠 fbm 絮状层次（不再是蓝色硬光束）；薄雾收紧半径（不再延伸到会投上天空的远处）。
- 修 High：书脊从"均匀纯色方块"改为**宽窄/高矮/进深各异 + 偶尔歪斜/抽出半截 + 做旧降饱和**；物件放大 + 提高自发光/金属度（陈列台不再"空"）；灯笼加**柔和光晕 + 假体积光柱**（光在薄雾里散开）；抬环境/月光、减暗角，救回死黑暗部。
- 结果（引擎内实拍复测）：低头是整片星海、暖灯有倒影、银河柔和、白斑/蓝束消失；书墙有层次、物件可见。`tsc` 0 错、`vitest` 27/27。
- 加：**入场电影**——入场前从高处（y≈3.7）缓缓环绕俯瞰整座漂在星海上的回廊；点「进入」后约 5.5 秒 smootherstep **俯冲沉到水面眼平**、落在出生点，临近水面时脚下荡开第一圈涟漪，再交还控制（`PlayerControls`）。落定一帧（眼平俯瞰整片星海上的回廊）本身就是个想截图的机位。

**Round 3（又跑一轮 `/design-review`）→ 两条最高杠杆：星海密度/对称 + 清理"糖豆"杂物**
- 评审指出：① 脚下星海太稀、不成"海"，且中段一条灰白雾"地平线"把天水切开（参照 01 的杀手锏是上下镜像对称、地平线消失）；② 满水面的暖色"葫芦/糖豆"实心椭圆（= 水下镜像发光球 MirrorBeacons + 烛环 + 漂浮书纸芯自发光）读不出是什么、像渲染残留，是当前最廉价的出戏元素；③ 到达第一眼没有视觉主角。
- 修：**加密星海**——星点 4200→7200、约 4 成星拉向银河大圆聚成"有星粒结构的星河"（不再靠雾）、整体更大更亮；**消地平线缝**——水面菲涅尔掠射不透明度 0.6→0.3、sheen 压暗、薄雾几乎删光（0.32→0.1、贴身不延伸），天水合一镜；**清杂物**——删掉 MirrorBeacons、删掉烛环、漂浮书纸芯自发光降到近无、删掉灯笼那根硬边假体积光锥（只留柔光晕）。
- 加：观星台**听歌角**（毯子 + 靠垫 + 暖台灯，在留声机旁）——给登顶一个"坐下听肖邦"的停留理由。
- 结果（实拍复测）：低头是一整片干净、密、亮的星海，糖豆消失，天水近乎对称；`tsc` 0 错。
- **灵魂机制已落地并实拍验证**：`scene/SunkenThoughts` —— 每条思考在水下是一颗**暖金色**光点（在一整片冷色星海里，"你的念头是暖的"，一眼能认出，不和星星混）；**写下新思考的瞬间，一点光从写作台落下、沉进水里**（落水触发一圈涟漪），加入既有的思绪星座；**随涨潮上浮变亮、落潮下沉变暗**。数据驱动（按 thought 实体生成，位置由 id 哈希稳定），首帧惰性建状态保证内容一加载就显示。这同时点亮了"完成标准"的第 2 问（独一无二的钩子）与第 4 问（为何待 10 分钟）。
- 加：观星台听歌角（见上）。`tsc` 0 错、`vitest` 27/27。

**Round 4（最终验证轮 `/design-review`：无 Blocker，核心已落地，指出"差一步到封面级"）**
- 评审确认：星海密度/灵魂思绪点/银河结构三件大事都成立，F_arrival 已跨进"想多看一眼"；唯一最高杠杆缺口——**低头时脚下星海被深蓝玻璃压住、没真正透出来**（水面菲涅尔不透明度偏高、地平线没消失）。
- **修（决定性一改）**：水面菲涅尔不透明度从 `mix(0.04,0.3)` 压到 `mix(0.015,0.12)`、指数 4→5——几乎全程透明 → 整片**镜像星空透出**、上下对称、地平线消失。低头即一整片与抬头同密的星海，暖思绪点沉在其中。F_arrival/F_starsea 升到"封面级"。
- 修 High：暖灯光球过曝纯白 → 降自发光强度（2.6/2.4/2.2→1.4/1.5）、核心改暖琥珀色（看得出灯芯，不再死白）；Bloom 强度/阈值微调，光晕更软。
- 成品包：`references/_final/`（F_arrival/F_starsea/F_books/F_islands/F_deck/thought_motes/write_sinks 七张实拍 + `walkthrough.gif` 一段穿行 + `README.md`）。`tsc` 0 错、`vitest` 27/27。
- 评审留下的收尾项（非阻断）：~~听歌角再 cozy~~（已修：圆软蒲团 + 折叠薄毯 + 摊开的书 + 茶杯 + 双层地毯，"有人刚坐过"）；漂浮书与书墙书脊再加"被翻过"的高低错落/亮色跳动；物件辨识度（仍是基本几何体）。
- 成品包已刷新到最终状态：`references/_final/`（七张实拍 + `walkthrough.gif` 穿行 + README）。`tsc` 0 错、`vitest` 27/27。

## 第五轮 · 回应用户实机评审（2026-06-30）

用户进游戏后给了 5 条尖锐反馈。先用参考调研（R3F 手感/性能、Bruno Simon 级建模、去 AI 味高级前端）落地一份核对过代码的优先级清单，再按此修。

| 用户原话 | 根因（读代码定位） | 本轮修法 | 验证 |
|---|---|---|---|
| 往地上看一片白曝光 | 水面 alpha=0.015 ≈ 透明窗 → 低头直视的是一整片**全亮**镜像天空；加色星点 ×1.35 + Bloom 阈值 0.55 累加成白 | 镜像天空压暗到 `bright=0.42`（倒影本就该比真天空暗）；星点亮度收敛；Bloom 阈值 0.55→0.82、强度 0.9→0.62；水面低头加一层深水薄纱 `alpha mix(0.22,0.07)` | `lj_lookdown` 实拍：低头是深色星海 + 暖金思绪光点 + 月晕，**白曝光消失** |
| 视角/移动/扭头都不丝滑 | ①真帧率（MSAA×4 + 全屏 DOF + dpr1.5 + 2048 阴影 + 14.4k 星 sprite + dev 的 preserveDrawingBuffer）；②输入延迟（PlayerControls 双层平滑：脚步 damp→相机再 lerp，把走动/转头糊成"游"） | MSAA×4→SMAA；常驻档移除 DOF；dpr 1 起步爬升到 1.5；preserveDrawingBuffer 仅截图验证时开；悬停 raycast 节流到 ~11Hz；**去掉二级平滑**——相机直接坐在单层 damp 的脚步上，转头/俯仰 1:1 即时 | `tsc`/`build`/`vitest 27` 绿；手感修法即调研报告的首选方案（待用户实机确认帧率） |
| 点了物品不知道怎么退 | `.back` 在 `top:64px;left:18px`，被 360px 左面板压住、又没文字 | 退出移到**右上角**，`‹ 返回漫游 [ESC]`（雪佛龙 + 衬线 + 键帽提示）；面板外加透明幕布点击即退；首帧闪一下琥珀引导视线（Esc 本就可用） | DOM 实测：退出在 top:20/right:22，`.focus-backdrop` 在位 |
| 建模粗糙感强（去学 Bruno） | 全是直角 box + 纯色平材质 + 裸发光球 | 凡盒子→`RoundedBox` 倒角；留声机喇叭/灯杆/灯泡/茶杯/笔杆→`LatheGeometry` 车削轮廓；书脊倒角（RoundedBoxGeometry）；黄铜 `metalness1 roughness0.26` 真反射 Environment；书墙加暖补光读出层次；框描淡金边 | `lj_gramo` 实拍：喇叭花成主角剪影；`lj_books`：书墙暖而可读；`lj_overview`：灯成泪滴灯泡 |
| 交互窗口 AI 味重（要高级前端审美） | 教科书级 glassmorphism：blur(14px) 玻璃面板 + 单一琥珀强调 + 大圆角 + system-ui | 子代理按"图书馆印刷品"方向重写 index.css/Hud/BookEditor：**删尽 backdrop-blur**、删呼吸光 CTA、删琥珀填色（只作墨线/墨字）；衬线字体系（Cambria + Noto Serif SC，离线不走 CDN）；发丝墨线 + 四级墨色分层；写作面板＝一页纸，列表＝印刷目录 | DOM 实测：blur 元素 0；面板/正文 Cambria 衬线；圆角 0；仅 1 处柔影 |

字体取舍：调研建议 Google Fonts 的 Fraunces，但本仓"**离线 / 不依赖外部下载**"是不可动约束 → 改用用户 Windows 本机已有的衬线栈，不引 CDN。

**第五轮·续（同次评审的第二轮迭代）**——用户反馈"UI 还是 AI 味重 + 喇叭花不像喇叭花"：
- **喇叭花重做**：原来是浅口"漏斗"且内壁全黑。改成牵牛花轮廓——`CatmullRomCurve3`+`TubeGeometry` 的弯鹅颈 + 更深更急扩的 `LatheGeometry` 喇叭口（口径 0.47/深 0.635）+ 卷唇铜环 + 喉部塞一盏暖 pointLight 把内壁照成金的。3/4 侧 + 正面实拍确认一眼是留声机。
- **中文字体病根**：上一轮 `--cjk` 写了 `Noto Serif SC/Source Han/Songti SC`——这台 Windows **都没装**，全回退到 **SimSun**（最"默认脸"的宋体），这正是"还像 AI"的一大来源。实测本机装有**华文字库** → 改 `--cjk: STZhongsong, STSong …`（华文中宋/宋体），`--cjk-kai: STKaiti`（楷体，给入场说明/留白文案的书卷气），`--cjk-fs: STFangsong`（仿宋，眉题）。`--mech` 改 Courier 打字机体（机械标签/键位/日期）——**这条偏主观，若觉得"像代码/终端"易回退**。
- **去模板**：入场页从居中 hero 改成**左偏的扉页**（非对称背景 + 扉页阅读序）；ObjectForm/RecordPanel/RecentPanel 三个之前漏改的面板补齐 masthead 结构 + 去 emoji；面板加极淡纸纤维纹理；提示文案移到左下作旁注。
- 验证：`tsc -b` 0、`vite build` 通过、`vitest 27/27`、DOM 实测（中文落到 STZhongsong/华文系、退出右上角、blur 元素 0）。**注：DOM 界面无法截图**（隐藏标签页合成被节流，`preview_screenshot` 必 30s 超时），UI 观感最终由用户实机判。

## 还很弱的地方（诚实清单 · 待后续轮次）

- **帧率仍需用户实机确认**：本轮做了结构性提速（SMAA 替 MSAA、去常驻 DOF、dpr 低起、raycast 节流、去二级平滑），但隐藏标签页 rAF 被节流，测不出真实 FPS。场景里仍有 ~12 盏动态 pointLight（灯笼/浮岛/台灯）是前向渲染的实打实开销，下一步可把部分灯烘成纯自发光、或上灯数上限。
- **入场情绪弧**：俯冲入场 + 第一圈涟漪已做；更完整的"黑场→逐次点灯→字浮出"分段编排仍未做。
- **物件辨识度**：浮岛上的"物件"仍是数据驱动的基本几何体（cube/sphere + 颜色），缺"一眼认出是某人之物"。这是数据契约里的 content，需要更丰富的 primitive 组合或图标系统——本轮只升级了 stage（建筑/家具）建模，没动 content。
- **灯泡仍带一块加色光晕盘**，泪滴灯泡 + 车削杆已远离"裸球"，但近看光晕略 CG；真正的玻璃罩（MeshTransmissionMaterial）本轮**为保帧率刻意没上**（它是唯一会真吃 FPS 的建模升级），留待帧率确认后给 ~2 盏主灯加。
- 材质仍是程序化纯色 + 倒角/车削轮廓，**无 PBR 贴图/法线**；大面（书墙/台面）缺微观粗糙度变化。
- 潮汐振幅小（0.055m），肉眼难察"水在呼吸"。
- 触屏漫游为基础实现；移动端以"看"为主。

### 本轮已解决
- 低头白曝光（已实拍确认消失）；视角/走动双层平滑导致的发糊（去掉二级平滑）；退出找不到（移到右上角 + 点外即退 + ESC 提示）；UI 的 glassmorphism「AI 味」（重写成衬线印刷品风）；裸几何原型（倒角 + 车削轮廓 + 真黄铜）。
- 注："写下的字沉入水 / 随潮浮起"机制其实已实现（`SunkenThoughts`：写新思考时一点暖光从写作台落入星海、随潮汐沉浮明灭），前几轮清单里"未实现"是过时描述。

### 已解决（前述轮次）
- 互动 zones 已接（数据驱动：书墙思考点亮、浮岛物件、留声机转盘）；真实音频已接（水声 CC BY + 肖邦夜曲 PD，空间化在留声机、走近变响）；旧别墅文件已清理；HUD/种子文案已改潮汐图书馆；`tsc` 0 错、`vitest` 27/27。

## 验证（本轮证据）

- 浏览器内（WebGL2, 1280×720 freecam 实拍）：星海镜面水成立、书墙/观星台/浮岛清晰倒映、漂浮书随潮起伏、
  暖灯回廊 + 冷夜星空的暖冷对比成像、DOF/Bloom 出镜头感。多机位 4 轮迭代修正记录在上。
- 第五轮（2026-06-30）：低头白曝光修复、建模升级、UI 重写均经 1280×720 freecam 实拍确认（`lj_lookdown2`/`lj_hero`/`lj_gramo`/`lj_books`）；UI 改动经 DOM computed-style 实测（blur 0、衬线、退出右上角）；`tsc -b` 0 错、`vite build` 通过、`vitest` 27/27 绿。
- 待办：真实硬件上的 FPS 实测（隐藏标签页测不准）；`/design-review` 正式一轮；低端档逐级 ladder。

## 第六轮 · 回应实机评审 + 引入手工模型（2026-06-30）

用户第三轮实机反馈（六点）→ 处理：

| 反馈 | 根因 | 处理 | 验证 |
|---|---|---|---|
| 1a 退出聚焦是硬跳转 | 退出时 useEffect 直接写回 feet/yaw/pitch，下一帧漫游分支瞬间 `camera.set` | 加"回程过渡"：退出时记当前机位，0.55s smootherstep 缓动飞回漫游位姿再交还控制（与进入的 lerp 对称） | tsc/构建绿；无头无报错 |
| 1b 远处准心看不出对准了啥 | 命中检测 `raycaster.far=REACH(7m)`，超出即不报 | 检测距离提到 24m；新增 `hoveredInReach`：远处只报名字+「走近」（淡字/准心微亮），近到 7m 才琥珀色+「ENTER」+可点 | store 字段实测生效 |
| 3a 移动太慢 | `SPEED=3.0` | → 4.3（Shift 冲刺 ≈7.3） | — |
| 3b 水波太白 | 涟漪加色 `#cfe0ff`(近白) + 峰值 1.4/透明度 0.6 | 改冷蓝 `#8fa9d8`、峰值 0.9、透明度 0.4 | — |
| 2a 为何不采纳"生图→2D→3D" | 见正文 | 正面回答：①撞你写的不可动#3（模型自给自足/不外联）②此环境跑不动那条流水线 ③产出多是又重又"AI 味"的 blob，拼不进有镜面水的舞台 ④Bruno 走的是低多边形+材质灯光，不是 AI 网格 | — |
| 2b 建模仍丑 | 程序化几何上限 | **用户选「方案 A」**：松绑不可动#3，引入手工 CC0/CC-BY 低多边形模型，离线打包 | 见下 |

### 不可动#3 的本轮松绑（用户授权）
- 原则"模型/HDRI/贴图自给自足、不外联"**仅就模型一项**放开：允许把**艺术家手做的** CC0/CC-BY 低多边形 GLB **离线打包**进 `public/models/`，**运行时仍 100% 离线**（与音频 CC-BY 例外同等待遇）。其余（HDRI/贴图、运行时外联、LLM）不变。
- 用户附加约束：**修改而非照搬** + **做好版本管理**（远端 github.com/king157419/myworld）。

### 留声机（首个落地，替换最被诟病的"喇叭花"）
- 选 Don Carson "Gramophone"（poly.pizza, **CC-BY 3.0**，1738 tris，10 占位材质）；署名见 `public/models/CREDITS.md`。
- 二次创作（非照搬）：① 10 材质按名重映射到调色板（黄铜喇叭/深木箱/黑胶；刺红唱标→暖琥珀），克隆材质不污染 useGLTF 缓存；② 喉部暖光照亮喇叭内壁；③ 包围盒归一化（缩放 1.32m、底面贴台、xz 居中，与原 pivot 无关）；④ `RecordPlayer` 旋转碟从悬空 y2.19 下沉到模型唱盘面 y1.71（压在唱针臂下）。
- 验证：1280×720 freecam 无头实拍（`gram1/2/3`）确认喇叭花成立、唱盘对位、暖冷氛围在；`tsc` 0、`vite build`、`vitest` 27/27 绿。提交 `5687d0f`。

### 版本管理
- `git init` + 基线提交 `66ce793`（76 文件，含本轮 1a/1b/3a/3b 修复）；`origin` = github.com/king157419/myworld（空仓，可净推）。
- `git push` 被本机安全钩子拦截 → 由用户手动执行 `git push -u origin main`。此后逐次原子提交。

### 待用户判定
- 留声机这条"手工模型"路线是否就是想要的观感？确认后按同法扩到其它道具（灯具/坐垫家具/书/陈列物件）——优先级待用户点。
- Courier 等宽标签、移动步速 4.3、水波冷蓝值，均为可回退的主观项。

## 第七轮 · 审计驱动的作品级改进（2026-06-30）

方法升级：用 **Playwright 真实浏览器**（非隐藏预览页，截图不再挂）+ DevBridge freecam 摆机位实拍 →
产出 `AUDIT.md`（七机位、严重度分级）；后台 **workflow** 挖标杆+技法 → `BENCHMARKS.md`（W1..W7 重实现配方）。
据此最严重者先修，逐项原子提交、逐项真机验证。

| # | 弱点 | 修复 | 提交 | 验证 |
|---|---|---|---|---|
| B3 | 喇叭内壁黑洞 + 金属平黑 + 过曝白斑 | 查明喇叭=mat19：降金属度+暖自发光+双面；口内补暖光；钢件提糙。Lighting 加厚 IBL（暖灯池/暖反射 ring/冷侧/暖地，res256）+ 轻抬环境 | 2c0bd89 | 实拍内壁发金、黑洞消失、未过曝 |
| B1 | 灯=扁平圆盘棒棒糖 | 加钟形车削灯罩罩住灯泡（外冷内暖双面），光晕收紧 | bde63e8 | 实拍成"立灯" |
| B2/C4 | 一切漂浮/书架透星空/书悬浮 | 书墙加沿弧曲面背板（墙成墙）；浮岛发光环收细+落水接光（接地） | 37ea4f8 | 实拍背板封缝、浮岛落地 |
| A2 | 空气墙"弹回来" | 碰撞 径向弹出→速度投影滑行；落点直采不再 damp 追逐 | 00b5a79 | 单测复现+回归（28 passed） |
| A1 | 聚焦不以物件为中心 + 退出硬切 | focusPoint=命中点/e.point 为中心（dist 收近）；退出转场逐帧采样确认本就生效，时长 0.55→0.7s | a9aa912 | 逐帧采样：z 平滑 -6.9→6.6 |
| C2 | 低头边走水波抽搐 | 根因=每0.5单位生成(8.6/s)冲爆14格缓冲、环被中途覆盖。放慢到1.2单位(3.6/s)+RIPPLE_MAX 20 | 70cf82b | 缓冲容量 ≥ 存活数，不再提前覆盖 |

### 完成标准 · 强制函数四问（诚实作答）
- **哪 3 个瞬间会让人想截图？** ① 低头那一刻——脚下整片星海、走一步荡开一圈涟漪（"原来我站在星空上"）；② 登上观星台、暖光留声机的黄铜喇叭口对着你、内壁发金，背后是冷星海；③ 书墙暖灯下，一盏立灯把一排旧书脊照暖、墙后是夜空。
- **哪一个元素是别的 build 想不到的？** "走在镜面星海上的读书回廊"——把"内心世界"具象成冷的星海水 × 暖的灯与书，且**思考会沉进水里、随潮浮起**（SunkenThoughts），是这个作品独有的隐喻。
- **进门头 60 秒的情绪弧？** 入场扉页（静）→ 点「进入」从高空环绕缓缓沉到水面眼平、脚下荡开第一圈涟漪（惊）→ 暖灯回廊与冷星海同时铺开、留声机低低转（暖/定）。
- **一个人为什么愿意待 10 分钟？** 因为它"随你生长"：写下的思考沉进星海、收藏的物件立在浮岛、喜欢的音轨在留声机上转——回访时空间和你不同。护城河在"生成之后"。

### 还很弱 / 本轮未做（诚实）
- **统一氛围光/体积光（W4 残留）**：暗部已抬、IBL 已厚，但缺真正的体积光锥/丁达尔——BENCHMARKS 已列 drei volumetric SpotLight / three-good-godrays(zlib) / raymarching 三条路，留下一轮。
- **C1 水面"存在感"**：仍偏纯透明（保"走在星海上"的签名，未敢重压），可加极淡法向扰动+掠射反光。
- **散落的漂浮书**：作为"记忆如水"意象保留，但密度/高度略散，待收。
- **2a 更多音乐**：用户想要更多曲目（明确说好听）——需再引入 CC0/PD 音轨并做切换，未做。
- **2b 望远镜互动**：用户说"可再等等"，仍是静态摆设；候选"点击有回报"。
- **4b 台阶体感与台阶模型未严格配准**：能上下，但 walk.ts 离散台阶高度与视觉/相机平滑未对齐，待精修。
- **1 动画整体仍可更顺**：本轮修了卡顿大头（碰撞/聚焦/退出/涟漪），但"丝滑"是持续目标。
- 真实硬件 FPS、`/design-review` 正式一轮，仍待。
- 不自我认证"美/完成"——交由用户判。

## 第八轮（用户复测反馈：台阶瞬移 / 边缘弹回 / 聚焦"莫名其妙"视角 / 退出无动画 / 音乐没加）

> 用户明确：灯光和音乐**这一轮就做**（"我什么时候说过可以等？"）；台阶也做；做好版本管理。
> 方法：先接真实浏览器（Playwright + DevBridge）**看清现状再改**，每个都真机验证，不提前宣布完成。
> 研究支撑：跑了一个多智能体 workflow → [BRIEF.md](./BRIEF.md)（图→3D 老实结论 / PD 音轨直链 / 台阶碰撞 + 聚焦取景数学）。

### 根因（都是"看"出来的，不是猜的）
- **聚焦"莫名其妙的视角"**：`PlayerControls` 用 `tmpObj.lookAt(center)`，而 `tmpObj` 是普通 `Object3D`——
  three.js 对**非相机**物体的 `lookAt` 会交换 eye/target，导致相机**正好背对**所点物件（实测 worldDir 与
  指向留声机方向 dot≈-1，截图里在看书墙/星空）。这是 2a 的真根因，intro/exit 用 `camera.lookAt`/欧拉所以只有聚焦坏。
- **退出"没有动画"**：转场设置放在 `useEffect`（`focusedZoneId` 变化后**晚一帧**才跑），中间那一帧漫游分支
  先把相机写回脚步位 → 直接跳。之前"逐帧采样说本就生效"是撞上了竞态的好的一侧。
- **上坡"瞬移上去" / 从侧面蹭上去**：支撑高度查询在竖直方向**无界**——离散台阶 + 广场(y=0)候选并存，跑快时
  一帧越过几级、或从侧面撞上高处台阶被直接抬上去。
- **台面边缘"空气墙向后瞬移"**：`clampToWalkable` 走出台面足迹后**径向投影回半径 8 的水圈**，把人从 z≈-10 猛拉到 -8。
- **"改成圆弧台阶却没看到"**：上一轮改的是书墙背板，不是台阶；台阶仍是 8 个方块 → 用户没看到变化，且体感不配。

### 本轮修复（逐条真机验证）
| # | 用户报的问题 | 修复 | 验证 |
|---|---|---|---|
| 1b/4b | 上坡瞬移 / 侧面蹭上去 / 台阶体感不配 | 离散台阶 → **连续线性坡道** `rampHeightAt(z)`（按 z 单调 → 不可能整级瞬移）；加 `MAX_STEP_UP=0.42` 门限（撞高坡壁像墙挡住，轴分离滑行）；比的是**起点地面**而非阻尼视高（否则跑步上坡误判撞墙） | 单测复现+回归；跑步上坡逐帧抬升 ≤0.42、顺利登顶 |
| 1a | 台面边缘向后瞬移 | `clampToWalkable` 改**最近可行走区**夹取（不再径向投影回水圈）；背沿/侧沿都停在边上 | 单测：从 z=-10 推到 -13 → 停在 -10.8，不再被拉到 -8 |
| 1c | 看不到"圆弧台阶" | Gallery 用**楔形坡道几何**（= 碰撞坡道本身）+ 薄踏条 + 坡脚暖光，碰撞与视觉**逐点一致** | 侧视实拍：坡道贴合、踏条随坡 |
| 2a | 聚焦不正对物件 | 每区 `FOCUS{center,radius}` 包围球，按 fov 反算取景距离**框住主体**，从玩家当前一侧切入；朝向用 `Matrix4.lookAt`（相机约定）修反向 bug | 实拍留声机居中发金；dot=1.0 正对；不再看书墙 |
| 2b | 退出无动画 | 进/出转场移进**帧循环内检测**（消除 effect 晚一帧竞态）；进/出统一**指数阻尼**缓动（无定时器 → 不受隐藏页/掉帧节流影响） | 帧内采样：退出 z 平滑 3.18→4.07→5.84→6.37→6.6，控制交还 |
| 2c/3a | 音乐没加 / 想要更多 | 留声机**曲库**：4 首 PD/CC0（肖邦夜曲 Op9-2/Op9-1/升c小调遗作 + 萨蒂 Gymnopédie No.1），空间化、放完自动接续、面板/HUD 可切换；UI 不再谎称"生成式爵士" | 4 曲加载 0 报错；playTrack/next/prev 切换正确；面板实拍曲目单 |

### 一个关于"图→3D 免费 API"的正面回答（用户反复问）
不是不做，是**没有一键的免费路**（详见 BRIEF.md）：能本地跑、许可干净（MIT）的模型（TRELLIS/InstantMesh/SF3D）要 8–16GB+ N卡；
不要 GPU 的托管免费档（Meshy/Tripo）输出是 **CC-BY 且公开**（要署名）；个别（Hunyuan3D/FLUX-dev/Zero123）有许可雷区。
**最现实一条**：Meshy 免费 image→3D 出 GLB（CC-BY 署名）或本地 TripoSR（MIT，可 CPU 慢跑）→ Blender 清理改造 → 打包 GLB（运行时仍离线）。
留声机 GLB 就是这条路的先例。下一轮可给望远镜/小物件走一遍。

### 仍然弱 / 下一轮（诚实，不自我认证）
- **统一体积光/丁达尔**：仍是最大短板（BRIEF/BENCHMARKS 有三条路），未动。
- **望远镜互动**：仍是静态摆设——候选下一轮用图→3D 管线 + "点击有回报"。
- **水面存在感 C1、漂浮书密度**：未动。
- 坡道虽连续顺滑，但仍偏陡（42°）；可再放缓或做真正的弧形平面。
- 真实硬件 FPS、`/design-review` 正式一轮，仍待。
- 不自我认证"美/完成"——交由用户判。

## 第九轮 · 结构性重构（Fable 接手）

用户指令："重构这个项目。"多智能体评审（6 批评者 + 16 对抗验证，~119 万 token）+ 亲读全部载荷文件，
10 项确认 / 6 项被验证否决（其中 3 项是本轮开工后已抢先修掉的）。11 个原子提交，每刀改前浏览器实证。

### 总判（诚实）
代码库**结构上是健全的，不需要重写**——theme 单源、walk 纯函数、数据契约、测试都在。烂在四类局部：
死代码与谎言注释、真相源分叉、god-file、以及三个"接了 UI 却没接管线"的死功能。逐项手术，不推倒。

### 修了什么（每项都有根因与实证）
| 类别 | 具体 |
|---|---|
| **真 bug** | 唱片机转碟/辉光绑在全仓库无人写入的 `playingTrackId` 上——上线以来从未转过。改绑 `useAudio.musicPlaying`。 |
| **契约违反** | `FOCUS`/`ZONE_ANCHORS` 按字面量 id 索引：改名 id 的合法存档点聚焦会飞向广场原点。改按 zone type 解析（实证：全改名世界聚焦留声机 2.82m 正对）。 |
| **死功能复活** | `room.mood` 有持久化/校验/UI/测试名，场景零消费——四个心境按钮是视觉空操作。config/moods.ts 单源 → Lighting（FogExp2/环境光/暖灯）+ Atmosphere（薄雾/雨夜远雷，顺便复活死了很久的 `thunder()`）。cool 档=改前基线。 |
| **死代码** | Room.tsx（无人 import）、STEP_RISE/RUN、focusPoint（只写不读）、interactables 簿记数组、Water 空 low 参数。 |
| **谎言修真** | tsconfig 从未开 strict（文档一直宣称"严格档"；开启后 0 错误）；engine 硬编码留声机坐标+假"循环依赖"注释；Water 注释描述不存在的 MirrorBeacons；联合类型三处手抄归一。 |
| **god-file** | Gallery(526行)→scene/gallery/ 八件+profiles/materials（黄铜 4 组漂移参数收敛、随机种子化、听歌角锚定 GRAMOPHONE）；PlayerControls(349行)→input/cameraDirector(+6单测)/audioListener/编排。 |
| **性能** | 思绪光点 60 useFrame+120 透明 draw → 1 useFrame+2 InstancedMesh（加色混合下色×α≡opacity α）；听者同步位姿未变跳过（原每帧 9 个自动化事件）；准心 raycast 维持 11Hz 节流。 |
| **音频** | 水声独立起播（原被 Promise.all 拴到最慢曲目后）；曲库按需解码（PCM 常驻 ~数百MB→≤2首）；坏轨自动跳且 UI 不再谎报"播放中"；start 可重入（resume 路径原不可达）。 |
| **UI** | 三面板五段式模板→useEntryForm（载入只 key selectedEntryId，草稿不再被冲）；Hud 拆四件；notice 定时器竞态；最近面板聚焦让位。 |
| **单测新发现** | 抽出 cameraDirector 后单测立刻揭出：俯仰夹取是 clamp 后 normalize，会被归一化顶回——按注释承诺修真（夹 y 重配水平分量）。38/38。 |

### 本轮明确不做（留给用户裁决）
- 体积光/丁达尔（最大观感短板，BRIEF.md 有三条路）；望远镜交互（候选图→3D 管线首发）；
  坡道仍 42° 偏陡；水面存在感；index.css 死规则清扫（低价值高误伤）；真实硬件 FPS；/design-review 正式一轮。

## 第十轮 · 观感升维：广阔 / 美丽 / 精致 / 丝滑（Fable）

用户指令："我希望世界更加广阔、渲染更加美丽、物品更加精致、视角更加丝滑。"
起手 12 机位实拍审计（AUDIT.md 全量重写）→ 6 刀原子提交，每刀浏览器实证后落库。
社区研究：6 域并行研究员（相机/物件/水 3 域完整回收，见 BENCHMARKS.md）+ 标杆挖掘。

### 总判（起手审计）
第九轮修的是骨，第十轮的病全在皮上，且相互耦合：**水面存在感≈0 是概念地基塌了**（"走在星海上"
读作"漂在外太空"），**世界没有远方**（四望虚无、一眼看尽），**中间调死黑**（灯看着都是关的）。
三者一起修，画面才立得起来——单修任何一个都看不出效果（水面反射的是光，远方衬的是地平线辉光）。

### 六刀
| 刀 | 内容 | 实证 |
|---|---|---|
| **水（W1）** | 高配 MeshReflectorMaterial 真平面反射："黑镜"配方（metal 1 + 暗蓝黑 albedo → specular 被染暗，月光白爆柱变克制月路、暖点光只留暗金光潭），opacity 0.5 透出水下镜像星海，fog=false（雾会把远水染成奶色）。低配保留菲涅尔玻璃水（零 RT）。思绪光点 renderOrder 4 = 荧光透水而出。 | 反射开启 397→431 calls；倒影/月路/光潭逐张实拍 |
| **光（W3+F1+F4）** | 灯罩自发光 0.45→1.1（原先"灯都像关的"）、灯泡 2.4→1.6 收白蛋、Sprite 渐变光晕换加色球；环境地板 0.42+hemi+25%、书墙弧两端补灯；env formers 加厚 ~40%；喇叭死黑真根因是**钟口内壁在喉光射程外**（raycast 实证，不是材质/法线/阴影）——内壁光挪进钟口；星光柱 Starfall（弦长假体积 shader，BackSide，雨夜熄灭） | 书墙实拍脱胎换骨；喇叭特写全金 |
| **远方（W2）** | Vista 远景层：26 座 7 棱锥群岛剪影（1 InstancedMesh，+Z ±32° 留空="有一个方向是纯粹的远方"）、呼吸灯塔（4.2s pow³ 脉冲）、7 点漂流水灯、Sky 地平线辉光带（exp(-y·10)）。全立在 y≈0 → 反射把纵深写进水里 | 台顶俯瞰实拍：群岛+光柱+暖廊同框 |
| **手感（H1+F2+F3）** | 惯性（起步 0.25s 满速/停步略快、撞墙速度同步）、步伐包络+肩部横移（不转 pitch 不晕）、FOV 随速微张 +2.6°；台上道具碰撞（留声机/台灯/望远镜——原可走进喇叭腔看背面剔除碎片）；聚焦避障 computeFocusPoseClear（方位候选+castShadow 遮挡集，3 条新单测） | 一路 W 精确挡在 0.94m；聚焦 2.82/6.70/6.17 与理论逐位同、避障后 facingDot 仍 1.0、退出零漂移 |
| **精致（P 簇）** | 光点晕圈换点精灵（原是近看穿帮的加色八边形）；书墙 ~9% 平摞书+弧端封板；写作台墨水瓶+待读书堆；黑胶高金属低粗糙（哑黑侧看=黑洞）；入场页左实右虚透出环绕世界；HUD 底栏加大 | 逐项实拍 |
| **审计+文档** | AUDIT.md 全量重写（12 机位）；DEV 画质钉死 lj_quality（节流页签 PerformanceMonitor 必误判降级，不钉死永远验不到高画质路径） | — |

### 强制函数四问（DECISIONS 必答）
- **哪 3 个瞬间会让人想截图？** ① 台顶回望：群岛剪影、星光柱、暖書廊和它们的倒影同框；② 入场页：标题右侧世界在缓缓环绕；③ 黑胶角特写：全金喇叭+转碟+灯晕。
- **哪一个元素是别的 build 想不到的？** 星光柱落在广场中心、你的思绪光点沉在它脚下的水里——"星光落下来的地方就是念头安息的地方"，这是把数据（用户思考）和舞台（体积光）焊在一起的一笔。
- **进门头 60 秒的情绪弧？** 题词页右侧世界若隐若现（好奇）→ 点进入，运镜沉到水面、脚下荡开第一圈涟漪（"原来我站在星海上"）→ 抬头看见远方群岛与灯塔呼吸（世界比我大）→ 走向暖光（归属）。
- **一个人为什么愿意待 10 分钟？** 写一段思考看它沉进水里发光；放一张黑胶看碟转、听空间化的声换位；上台顶看自己的世界的全景倒影；四种心境换四种夜。
### 本轮明确不做（延后，留给用户裁决）
- 涟漪→distortionMap 管线（脚步真实打碎倒影——研究员已给方案，是水面的下一击）
- 程序噪声 roughnessMap"旧物感"、MeshPhysicalMaterial 三件套（clearcoat/sheen/anisotropy）
- 望远镜交互（图→3D 管线首发候选）、坡道 42°、真机 FPS、/design-review 正式一轮
- 月光 glitter path 专用 shader（当前月路来自 specular，够用但不可艺术指导）

## 第十三轮 · 留声机重雕 + 日记入世界（loft 打磨优先）

用户拍板：先把第一个场景打磨好；留声机"形状还可以但光照渲染很烂"，要雕塑感；私人日记（dairy.zip，55 篇 docx）作为内容进世界。

### 留声机：自制程序化雕塑替换 poly.pizza 低模
- `tools/bake/build_gramophone.py`（headless Blender）：参数化牵牛花喇叭壳（指数扩口 k=3.4、8 瓣花瓣调制、
  缘口波浪、solidify 双层壁内外分材质）+ 鹅颈贝塞尔扫掠 + 倒角木箱 + 唱臂/摇柄/毛毡，36,740 tris，
  smart-UV + Cycles AO 同管线烘焙。材质名语义化（brass/brass_inner/…），REMAT 按名重映射。CC-BY 署名义务随替换解除。
- 资产 pivot 契约：唱盘中心在原点、底面 z=0、口朝 +Z、总高 1.374——运行时**只做高度归一+贴地**，
  不再 xz 重心居中（喇叭前倾会把箱体推离锚点，旋转碟就不对位了）。
- **踩坑三连（都写进脚本注释了）**：① `primitive_cube_add(size=1)` 边长就是 1，scale 别再除 2；
  ② `from_pydata` 环带 quad 绕向不定 → 必须 `normals_make_consistent` 再 solidify（offset=-1 保参数面为外壁），
  Cycles 双面渲染看不出坏法线，three.js 单面剔除直接"黑洞剪影"；③ **AgX 会把 ≤1 的自发光压到近黑**——
  夜景+逆光位的金属壳必须给真正的主光（本轮 5 盏：铃口暖芯/喉部/来客侧键光/左后冷月轮廓/顶背月光），
  emissive 只能当底线不能当照明。这条同样解释了旧模型为什么"烂"。
- 实拍验收：访客仰视角铃口是发光金花（tools/bake/work/gram13_final_*.png），壳顶高机位仍有重黑块——挂账。

### 日记进世界：本地收件箱（隐私红线）
- **仓库是 public 的 → 日记绝不进 git**：`dairy.zip`、`public/inbox/` 进 .gitignore；真相落在 IndexedDB（用户数据侧）。
- 管线：scratchpad 脚本解析 docx（正文 + 文件名日期 > docProps created > zip 时间）→ `public/inbox/loft.json`
  （55 条 thought，id=`diary-<sha1>` 幂等）→ 启动/切场景时 `absorbInbox()` 经 `io.parseEntryBatch` 守门吸收。
- 守门复用：io.ts 抽出 `cleanEntries` 供 parseSavedWorld / parseEntryBatch 共用；zoneId 必须指向真实功能区。
- 幂等两道锁：文件侧按 id 去重 + `set` 回调内按落笔瞬间状态二次去重（StrictMode 双挂载/并发 absorb 不翻倍）。实测两次刷新 69/55 不变。
- 书脊上限 24→48（61 条思考时书墙密度跟上；instanced 单 draw 无感知）。

### 工具链：blender-mcp 已装（会话重启后可用）
- `claude mcp add --scope user blender -- uvx blender-mcp`；插件已装进便携 Blender 并保存偏好。
- 用途定位：**交互式捏资产**（望远镜、湖石等探索型造型）；批处理烘焙仍走 headless 脚本（可复现、可版本化）。
- 使用前提：Blender 开着 + 插件 server 在跑（`blender.exe` 起 GUI 后 N 面板 BlenderMCP → Connect，
  或 headless `--python-expr "import bpy; bpy.ops.blendermcp.start_server()"` + keep-alive）。

### 本轮挂账
- 壳顶高机位重黑块（AgX 夜景的固有对比；候选解：月光 directional 自阴影排查 / 壳顶专用渐变 env）。
- 望远镜交互形式等用户拍板；两个新场景的粗糙处继续冻结，loft 打磨优先。

## 第十二轮 · 双场景通宵构建（进行中）

用户指令（通宵自主，整晚不等不问）：在现有管线上建**两个交付级场景**——A·雨夜阁楼（西式，门厅→木楼梯→阁楼，
屋里暖窗外冷）、B·雾中山居（中式庭院→书房，水墨灰绿宣白）；场景配置切换；个人印记种进内容；
参照双通道（网络真实照片为主锚 + Codex image_gen 补缺）；宁可 A 完美 + B 半成品。

### 架构三决策（开工前定死）
1. **场景选择器 = `world.room.style`**：类型里第一轮就预留了 `courtyard`；新增 `attic`；`loft` 保持=潮汐图书馆
   （历史名，用户存档零迁移风险）。io 校验同源数组自动跟随。
2. **每场景一份独立 SavedWorld**：Dexie 升 v2（worlds 按 style 键控、entries 加 scene 索引、meta 存 lastScene），
   旧 `main` 行迁移为 `loft`。导出/导入契约每场景各自成立；三 zone 契约每场景各守一份。
3. **场景注册表**：Stage 组件 + spawn + walk 求解器 + zone 锚点/皮肤 + （后续）音频配置；
   PlayerControls/cameraDirector/Zones 经注册表取当前场景数据，theme.ts 降为 loft 私有真相源。

### 模型分配纪律（用户指定，逐类记录）
| 任务 | 模型 |
|---|---|
| 主会话编排 / 架构决策 / 印记内容执笔 / 每轮审美评审 / 最终验收 | Fable（评审职能；印记是灵魂内容，不外包） |
| 架构骨架实现、场景 A/B 构建、参照收集、Codex 生图操办、音频收集 | Opus 子代理 |
| 机械杂活（如出现：批量重命名/格式转换） | Sonnet/Haiku |

### 场景 A 评审 · 第一轮（Fable，对照 references/attic 满分定义；证据 references/_audit/r12/r12_attic_v1_*）
v1（`6be6747`）动线全通（连续坡楼梯 1.6→4.6、后墙 -11.1 停步、19 条印记注种一字未改），但对锚七处失分 + 一处功能 bug，
已回给 Opus 修（按严重度）：F1 雨痕是"暖金大圆环+字符雨竖纹"非水珠拉丝（最伤，特写即穿帮）；F2 天窗夜空亮成 LED 面板
（冷侧吃掉暖侧，双色平衡破）；F3 屋顶横梁纯黑贴片不受光；F4 黑胶角死黑（唱机读不出，锚要求金属锐利暖高光）；
F5 门厅 spawn 视角 90% 纯黑（气窗冷光近黑）；F6 点亮书脊像日光灯管；F7 台灯下纸张过曝炸白。
B1：lastScene=attic 直接启动不落出生点（z=6.91 界外悬空）——瞬移只挂在 style 变化上，首帧未初始化。
v1 做对的也记下（保住别改坏）：暖光潭半径与近黑墙角、书墙塞放节奏、写字台的信/钢笔/墨水杯构图、楼梯口暖光牵引。

### 场景 A 评审 · 第二轮（证据 r12_attic_v2_*）
F1-F7/B1 修复实证：雨窗成了小水珠+蜿蜒拉丝（特写成立）、天窗深蓝灰夜+云噪不再是 LED 板、横梁受光、门厅楼梯逐级光潭
（"被楼上暖光牵引"成立）、书脊自色微光、桌面层次保住、直接启动落点 [0,1.6,5.1] 精确。
爵士曲库/黑胶底噪/第七级吱呀已接线（setLibrary 加性接口，loft 曲库零变化）。
余项三条（转 polish 轮）：A1 唱机本体仍吞光（封套边缘光已好）；A2 陈列柜点亮物件像灯箱；A3 微尘近看白方块。

### 场景 B 评审 · 第一轮（证据 r12_court_v1_*）
v1（`2fee0b4`）第一眼即成立：灰绿-宣白-墨黑锁色、三层雾（近石径清晰/中景褪色/远山剪影）、纸灯唯一暖点、
雨丝、瓦垄、檐口滴水、**墙上竹影**（directional 投竹竿影——彩蛋级）。动线全通（台阶 +0.55 连续、矮几合法碰撞）。
八处失分（转 polish 轮）：C1 水池=黑洞+霓虹白圈（最伤）；C2 卷轴空白无墨（印记可视载体缺失→canvas 竖排真句子）；
C3 松冠/湖石/檐底/博古架顶纯黑不受光（F3 同病）；C4 围墙纹理读作波纹铁皮；C5 月洞门内外空气透视断裂；
C6 天色偏白日、纸灯对比被摊薄；C7 雨丝偏白亮；C8 琴角上部漏看远山（几何缝）。

### 收尾 polish 轮回执（`ad7c7cb`/`6db0822`，证据 r12_attic_v3_* / r12_court_v2_*）
阁楼 A2 陈列物件琥珀化✓、A3 微尘软圆点✓；A1 唱机有结构可读但金属锐利高光仍未达锚（诚实挂账）。
山居 C1 涟漪灰绿细圈+池面菲涅尔✓、**C2 卷轴 canvas 竖排真墨迹**（「痴人」「心远」批注可读——印记从数据变成看得见的东西）✓、
C4 墙渍团块化✓、C5 月洞门雾幕接上空气透视✓、C6 天色压向黄昏✓、C7 雨丝淡灰✓、C8 封缝✓；C3 松冠/湖石仍偏暗块（部分改善）。

### 终验（Fable，全链）
门禁 45/45 + build ✓ + lint 仅既有 warning；loft 回归逐帧原样（星海/书墙/星光柱/曲库四夜曲自动恢复）；
按场景曲库实证（attic 三爵士⇄loft 四夜曲）；低配档三场景切换零报错；lastScene 记忆；直接启动落点精确；
dev 钉子已清（用户打开即自然画质档）。穿行 GIF：references/_audit/r12/walkthrough_{attic,courtyard}.gif（Pillow 拼帧，无 ffmpeg）。

### 强制函数四问（第十二轮）
- **哪 3 个瞬间会让人想截图？** ① 阁楼写字台：台灯下没写完的信、钢笔搁在末行、窗上雨珠拉丝外是深蓝夜；
  ② 楼梯半程抬头：阁楼暖光洒下台阶，书墙和天窗在上面等着；③ 山居进门第一眼：雾里石径通向亮纸灯的书房，远山三层剪影。
- **哪一个元素是别的 build 想不到的？** 卷轴上的墨迹是数据驱动的——他写的批注被 canvas 竖排成字挂在书房墙上；
  楼梯第七级会真的响一声，而书架上恰好有一条日记写着"我没修"。内容和舞台互相指认。
- **进门头 60 秒的情绪弧（阁楼）？** 昏暗门厅一盏边桌灯（压）→ 楼梯口暖光牵引拾级而上（期待）→
  登顶豁然：书墙、天窗雨、爵士与黑胶底噪（释放）→ 走近写字台读到没写完的信（亲密）。
- **为什么愿意待 10 分钟？** 三个世界装同一个人：在阁楼听雨写想法，在山居读他抄的诗，回图书馆看念头沉进星海——每处都能写能存，刷新还在。

### 本轮明确不做（延后，留给用户裁决）
- 唱机金属高光补强（A1 余项）、松冠/湖石提亮（C3 余项）、雨丝近灯染暖（逐实例分色）、竹竿摇曳、场景切换过渡黑幕
- 真人耳朵验音频（雨床音量配比/吱呀触发手感——无头环境无法代替）、真机 FPS、courtyard draw calls 实测
- 信纸聚焦阅读交互（当前是贴图级可读）、山居书房室内外遮挡（对比靠内容灯撑）
- 第十一轮遗留继续挂账：直接光进烘焙、ripple distortionMap、望远镜、42° 坡

### 参照双通道（回执）
- 通道一（主锚）：references/attic/、references/courtyard/ + 各自 SOURCES.md（出处/许可/锚定点/满分定义）。
- 通道二（辅助）：Codex CLI 内置 image_gen（ChatGPT OAuth，无 API key），提示词参考 EvoLinkAI
  awesome-gpt-image-2 仓库改写，产物 references/generated/ + PROMPTS.md 逐张记录。**AI 生成图仅作构图/氛围补充。**
- 个人印记正式稿：references/imprint/IMPRINT.md（Fable 执笔，实现时一字不改转 seed）。

## 第十一轮 · Blender 烘焙管线：分灯 lightmap 落地（Fable）

用户拍板："配 Blender 会不会更好？"→ 走 A 路线。本机无 Blender → 自装便携版 4.5.11 LTS（`D:\desk\ds\tools`，
零侵入不进注册表；国内网络三镜像试错 + 断点续传分段下载 + 绕过僵死代理 TUN 直连 WLAN 才拿下）。

### 总判
第十轮把实时管线能白嫖的都吃完了；下一个量级的门是烘焙。但 My Room in 3D 的**全烘焙无光材质**照搬不进来——
我们有数据驱动的动态内容（书脊/物件/转碟）必须和舞台同吃实时灯，全烘焙会让内容和舞台受光割裂。
本仓库变体：**只烘实时管线做不出的部分**（GI 反弹/软接触影/方向性遮蔽），直接光照与阴影全实时——
动态内容永远和舞台一致，无双重曝光，mood 契约原样保留。

### 管线六件
| 件 | 内容 |
|---|---|
| **导出** | devExport（仅DEV）：`userData.ljBake` 标签（static 烘+换 / emitter 只当光源 / occluder 只挡光 / content 灯永不烘）拍平世界变换导出 GLB；灯光序列化 JSON——**灯位灯强取自活场景，Blender 布灯零手抄** |
| **烘焙** | bake_shell.py：97 静态件合并（Lightmap UV2 smart-project）+ 529 遮光体 + 6 发光罩 + y=0 黑镜水面；OptiX 384spp×4 pass；R=暖灯间接(+罩直射，实时端本无面光源不双计) G=月光间接 B=天光全量 |
| **编码** | 每通道无色辐照度：99.7 分位定标 + sqrt 编码进 8bit PNG（压暗部 banding）；运行时 stored²，绝对亮度=K 旋钮（Blender W/m² 与 three candela 本不通约，贴图只当归一化分布） |
| **运行时** | BakedShell：onBeforeCompile 换 `lights_fragment_maps`——分通道调色调强（暖跟 lampMul 呼吸/天光跟 hemiIntensity/月光锚定），烘焙天光兼任 AO 压平坦 ambient/hemi/IBL 漫反射；PBR 镜面/env 原样 |
| **分层** | 高配 BakedShell 接管、四组件跳静态件（书墙立柱例外——Edges 描边是招牌细节，保程序化只当遮光体）；低配/产物缺席（meta.res=0）逐像素回退第十轮原样 |
| **试点** | gramophone smart-UV+AO 打通全链路（材质名 mat19… 原样保留 REMAT 不断）；喇叭壳 noAo |

### 数字
draw calls 431→**320**；tri ~666k；lightmap 2048² PNG 1.1MB + 壳 GLB 5.1MB（离线打包）；41/41 测试、tsc/build/lint 绿；
walk/聚焦/退出/三 zone 契约/雨夜（星光柱熄灭+暖通道随 lampMul 收敛）全部实证。

### 强制函数四问
- **哪 3 个瞬间会让人想截图？** ① 写作台灯下的书墙近景：灯洇进层板、缝隙自己暗下去（本轮新）；② 台顶回望群岛+星光柱同框（第十轮保住）；③ 黑胶角全金喇叭+接触影木箱（AO 新）。
- **哪一个元素是别的 build 想不到的？** 烘焙贴图三个通道各存一组灯的"无色辐照度"，mood 面板拨一下、整面墙的反弹光跟着心境呼吸——烘焙是死的，通道是活的。
- **进门头 60 秒的情绪弧？** 不变（本轮不动叙事）——但走近书墙时木头"接住"灯光的那一下厚了。
- **一个人为什么愿意待 10 分钟？** 不变；本轮买的是"待着不出戏"的底子。
### 本轮明确不做（延后，留给用户裁决）
- **直接光进烘焙**（真正换脸的一步：需 shadow catcher 或内容/舞台双光路设计——大改，等用户判）
- 月光间接通道改进（当前近空关死）；喇叭口暗区补灯；KTX2 压缩贴图（当前 PNG 1.1MB 可接受）
- 第十轮遗留继续挂账：ripple→distortionMap、noise roughnessMap、PhysicalMaterial 三件套、望远镜、42° 坡、真机 FPS

## 第十四轮 · 望远镜「看记忆」+ 六项打磨（loft 继续，Opus）

主人点单：望远镜要"优美 + 可交互"（选方案二·看记忆）、六项优化全上、建模用 Blender。

| 项 | 决策 |
| --- | --- |
| **望远镜模型** | `tools/bake/build_telescope.py` 程序化黄铜折射镜（锥形木三脚架+脚箍+撑盘 / 方位座 / 曲臂双抱箍 / 镜筒+雕刻箍环+遮光罩+物镜玻璃 / **星对角镜把目镜折向来客** / 调焦座+双轮 / 寻星镜）。仰 57°，物镜朝夜空、目镜迎人。Cycles AO 烘焙+smart-UV→`telescope.glb`+`_ao.png`（14760 tri）。语义材质名沿用留声机那套 + glass。`TelescopeModel.tsx` 重映射+真点光，替换 `Deck.tsx` 三根圆柱堆的旧镜。 |
| **「看记忆」交互** | 望远镜是**舞台件不是第四个 zone**（三 zone 契约不动）：`theme.TELESCOPE_ID` 哨兵登记为可交互件，命中 ENTER → `useWorld.telescopeActive`（与 focus 互斥）→ PlayerControls 阻尼飞到目镜后、望向物镜；ESC/返回退出。`MemoryScope.tsx` 圆目镜叠层：把 `type==='thought'`（含导入日记）按时间铺成"记忆星河"canvas，**同一股潮汐（`tidePhase`）载你在时间轴上前后穿行**（#4 时间旅行），也可手动拖时间刻度；点星读到那条记忆原文。纯数据驱动，星河可由数据重建。 |
| **沉字纪元显隐** | `SunkenThoughts` 每颗沉字归一到时间轴，与潮位相位近的纪元随潮**上浮+渐亮**（水面上的时间旅行回响），其余退回底衬绝不熄灭。 |
| **涟漪 distortionMap** | `Water` 高配把脚步涟漪场离屏渲成 256² 灰度贴图（红=环强度，与水面 circleGeometry 同一 UV↔世界映射），喂 `MeshReflectorMaterial.distortionMap`（distortion 0.22）——倒影星空在踩出的每圈涟漪处真实被扯动（叠层 RippleOverlay 仍管亮环）。清掉第十轮起挂账的 ripple→distortionMap。 |
| **入场编排** | 入场电影加"掠水拍点"：中途 `sin(πx)` 把相机压近水面再抬到眼平（仅有水场景，防非水场景穿地）+ 尾程二次落水涟漪；INTRO_DUR 5.5→6.4，视线从环视回廊落到眼前的路。 |
| **留声机壳顶黑斑** | 挂账项复查：铃口顶缘那片"黑"**主要是越过喇叭剪影看到的暗夜空**（射线实测：上半 58m 远=天空，下半命中内壁）——光照修不动。加一盏铃口后上方冷缘光+抬 brass emissive 至 0.42 做轻缓解；真正成片的暗只在**够不到的正上方机位**（观星台是最高可行走面，玩家只平视/略仰）。DoubleSide 实测无效已移除（不是剔除黑洞）。 |

### 本轮最大的坑：**烘焙鬼影**（记录，别再踩）
删了 `Deck.tsx` 的旧三柱望远镜（`ljBake:"static"`），高画质档却仍有一根黑筒挡住新望远镜。射线查出是 `Shell` 组里 `Material_13`（`#1c2430` metal .7）——**老镜筒被烘进了上一次的 BakedShell GLB**。CLAUDE.md 早写明"改了 static 件几何须重烘"：低画质导出 `shell.glb`(631 网格,无老镜筒)+`lights.json`(34 盏) → `bake_shell.py` 2048/384 → `bake:install` → 鬼影消失。**教训：删/改任何 `ljBake:"static"` 件，必须连带重烘 shell，否则高低画质不一致，旧几何以鬼影留在高配。** 重烘定标与旧值近似（warmScale 0.0148→0.0169，sky/moon 几乎不变），整场无回退。

### 验证（诚实）
环境两个交互浏览器面都不可用（真 Chrome 扩展没连；内嵌 Browser 面后台标签冻结 rAF/不建 WebGL）→ 改用 **Playwright 自带 Chromium**（rAF 正常）+ `__lj.advance()` 手动步帧 + `__freecam` 摆机位 + `toDataURL`→接收器存盘。实拍验过：望远镜在场（鬼影除后暖铜筒身/箍环/目镜迎客全清晰）、看记忆全链路（星座+潮汐穿越+点星读到真实日记）、60 颗沉字明暗有别、distortion 接线（0.22 + 256² 贴图 + USE_DISTORTION 宏）、留声机眼平好看、入场落定在 spawn。build/49 测试绿。入场"掠水"动画与涟漪扭曲的动态最好在实时游玩里看（静态帧看不全）。

### 仍挂账
真实音频每场景切库、月光间接通道、KTX2 压缩、直接光进烘焙、留声机壳顶正上方机位暗块（够不到，暂不追）、attic/courtyard 打磨（主人明确冻结至 loft 完成）。
