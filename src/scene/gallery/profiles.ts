import * as THREE from "three";

// 车削轮廓（LatheGeometry 用，绕 Y 回转）：整个回廊共用的一套"被建模过"的剪影。
// 曲面回转剪影＝"真实物件"，不是基本几何体拼出来的原型感。

export const V2 = (r: number, y: number) => new THREE.Vector2(r, y);

// 灯杆：细的车削木/铜杆，带一两处节。
export const POST_PROFILE = [
  V2(0.085, 0), V2(0.05, 0.05), V2(0.044, 0.22), V2(0.058, 0.34),
  V2(0.04, 0.5), V2(0.044, 1.55), V2(0.052, 1.78), V2(0.038, 1.9),
];
// 灯泡：泪滴状（圆底、微收顶），比完美球体远没那么"AI 默认"。
export const BULB_PROFILE = [
  V2(0.0, 0), V2(0.07, 0.03), V2(0.115, 0.1), V2(0.12, 0.18),
  V2(0.095, 0.27), V2(0.05, 0.33), V2(0.0, 0.35),
];
// 小台灯灯泡（更小）。
export const SMALL_BULB = BULB_PROFILE.map((p) => V2(p.x * 0.82, p.y * 0.82));
// 茶杯：杯壁 + 卷口。
export const CUP_PROFILE = [
  V2(0.0, 0), V2(0.04, 0), V2(0.048, 0.015), V2(0.052, 0.08), V2(0.058, 0.1), V2(0.05, 0.092),
];
// 钟形灯罩（车削）：底口宽、向上收窄。罩住灯泡——把"裸灯泡+大圆盘光晕"的棒棒糖换成有体量的灯。
export const SHADE_PROFILE = [
  V2(0.205, 0), V2(0.2, 0.03), V2(0.165, 0.1), V2(0.11, 0.19), V2(0.06, 0.265), V2(0.045, 0.285),
];

// 做旧书脊配色（BookWall 装饰书 + FloatingBooks 漂浮书共用）。
export const BOOK_COLORS = ["#7d3b2e", "#3f5a46", "#6b5630", "#34465f", "#5a3550", "#7a6a45", "#2f3e3a"];
